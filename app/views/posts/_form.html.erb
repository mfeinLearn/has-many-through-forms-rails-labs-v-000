<%= form_for post do |f| %>
  title: <%= f.text_field :title %><br />
  content: <%= f.text_field :content %><br />
categories:  <%= f.collection_select :category_ids, Category.all, :id, :name, {include_blank: "select a category"} %><br />

<%#= f.label "OR create a new category"  %>
<!-- nested forms in rails synonymous with fields_for .   -->
<!-- you must call fields_for on form builder object  -->
<!-- we are going to pass in the attribute that we are
going to be writing as a symbol. so in this case we want to
write the post catagory  -->
<!-- -->
<br>
Or, create a new category:
<%= f.fields_for :category do |cat|%>
<%= cat.label :name %>
<%= cat.text_field :name %>
<% end %>
  <%= f.submit %>
<% end %>
